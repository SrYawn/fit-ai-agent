# 文档维护策略

## 必须更新的场景
- 项目稳定知识变化时，更新 `openspec/project/*`。
- 开发流程或验证流程变化时，更新 `openspec/workflows/*`。
- 每个非简单需求都应维护 `openspec/changes/<change-id>/*`。

## 维护原则
不要把重要知识只留在 Agent 记忆、聊天记录或临时说明里。只要这些信息会影响后续开发决策，就应进入 OpenSpec。

## 变更记录完成标准
一个需求只有在以下内容都被记录后，才算文档闭环：
- 设计意图
- 实现摘要
- 验证证据
- 关键决策
