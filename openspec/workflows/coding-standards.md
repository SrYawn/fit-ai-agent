# 编码标准

## 工程要求
- 优先控制改动范围。
- 优先小而准的修改，不做顺手大重构。
- Agent 提示词必须和真实工具契约保持一致。
- 如需驱动下游逻辑，优先使用显式状态标记。

## 风险控制
- 专用 Agent 不应暴露过宽的工具面。
- 禁止伪造用户事实、用户名或健康数据。
- 动态查询条件必须优先使用参数化 SQL。
- 外部依赖失败必须作为一等控制流来处理。

## 修改 Prompt 时
- 确保提示词与工具签名一致。
- 确保在需要时明确描述降级行为。
- 避免鼓励模型进行猜测式推理。
