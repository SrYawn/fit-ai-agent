# 术语表

- OpenSpec：仓库中 `openspec/` 目录下的结构化文档系统。
- 变更记录：`openspec/changes/` 下按需求建立的单独目录。
- 落地数据：来自真实依赖系统（如 MySQL）的数据。
- 降级模式：在依赖缺失或输入不足时显式进入的回退处理模式。
- MCP：Model Context Protocol，用于对接工具能力。
- RAG：检索增强生成，依赖健身知识库返回辅助信息。
