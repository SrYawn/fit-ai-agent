# 需求说明

## 摘要
调整论文 `毕业论文初稿.md` 中 `5.4 性能测试` 小节的测试方案描述。

## 背景
现有表述将响应时间测试与并发测试混合在一起，且与当前项目接口结构不完全对齐。需要按新的论文写法拆分：
- `5.4.1` 只做串行响应时间测试；
- `5.4.2` 使用 JMeter 对流式工作流接口进行 10 并发测试。

## 范围
- 更新 `5.4.1 响应时间测试` 的测试方法、测试对象和分析说明。
- 更新 `5.4.2 并发性能测试` 的测试方法、参数配置和截图展示建议。
- 保持接口路径与当前实现一致。

## 非范围
- 不在本次变更中实际执行 JMeter 压测。
- 不生成真实性能数据和截图素材。

## 验收标准
- `5.4.1` 明确为串行接口响应时间测试。
- `5.4.2` 明确为 JMeter 对 `GET /api/api/fitness/workflow/execute/stream` 的 10 并发测试。
- 文案中的接口地址与当前代码实现一致。
