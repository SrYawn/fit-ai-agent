# 实现记录

## 改动文件
- `毕业论文初稿.md`
- `openspec/changes/2026-04-thesis-performance-test-plan/*`

## 行为变化
- 将论文 `5.4.1` 改为串行接口响应时间测试方案。
- 将论文 `5.4.2` 改为使用 JMeter 对流式工作流接口进行 10 并发测试。
- 在 `5.4.2` 中新增了截图展示建议，便于后续插入论文实验截图。
- 在 `5.4.1` 中补充了同步与流式工作流接口的完整 `curl` 示例，便于复现实验。
- 为流式 `curl` 示例补充了 `-w` 时间统计，便于记录首事件返回时间与总耗时；为同步 `curl` 示例补充了总耗时输出。
- 将 `5.4.1` 的测试对象细化为闲聊、动作指导、计划生成、陪伴激励、用户画像同步和用户画像流式六类场景。
- 为排查 `fitness-db-mcp-server` 的 JDBC 连接失败问题，统一将主应用和 MySQL MCP 服务的 JDBC 地址切换为 `127.0.0.1`，并补充 `allowPublicKeyRetrieval=true`。
- 保留主应用启动 MCP 子进程时的 `-Dlogging.pattern.console=` 参数，因为 stdio MCP 传输要求 stdout 只输出 JSON 协议消息；移除后会被 Spring Boot 启动日志污染。

## 与设计不一致的地方
- 无。

## 后续说明
- 本次变更仅调整测试方案文案，未执行真实性能测试。
- 后续应根据该方案补充真实测试结果、截图和分析数据。
