# 决策记录

## 决策 1
- 背景：现有 ReAct Agent 通过同步 `call()` + 手动工具执行循环工作，无法直接把整个工具调用过程改成低风险的 token 级流式输出。
- 决策：采用“两阶段流式”方案，先同步完成路由和工具调用准备，再通过 `ChatClient.stream().content()` 流式输出最终用户可见答案。
- 影响：SSE 正文变为真实 token 流，且改动集中在最终回答阶段；代价是流式最终文本与同步最终文本可能存在轻微措辞差异。
